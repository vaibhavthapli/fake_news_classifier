# fake_news_classifier

Bild a system to identify unreliable news articles, just by watching the news title.

We use Bidirectional LSTM for test that this algorithm work on sequential-to-vector data.
